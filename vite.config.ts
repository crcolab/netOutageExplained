import { defineConfig } from 'vite'

export default defineConfig({
  // GitHub Pages deploys to https://<user>.github.io/<repo>/
  // Change 'netOutageExplained' if your repo name differs
  base: '/netOutageExplained/',
})
